<!DOCTYPE html>
<html lang="en">
  <head>
    <title>
      {{ eleventyNavigation.title }} | Web Audio Samples
    </title>
    <meta charset="UTF-8"/>
    <meta name="keywords" content="{{ metadata.keywords }}">
    <meta name="description" content="{{ metadata.description }}">
    <meta http-equiv="x-ua-compatible" content="ie=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover"/>
    <link rel="stylesheet" href="{{to_root_dir}}styles.css"/>
    <link rel="apple-touch-icon" sizes="180x180" href="{{to_root_dir}}favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="{{to_root_dir}}favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="{{to_root_dir}}favicon/favicon-16x16.png">
  </head>
  <body>

    <div>
      <div id="header" class="py-12">
        {% if not hide_header %}
          <div class="w-full px-6 md:max-w-4xl md:mx-auto">
            {% include "header.njk" %}
          </div>
        {% endif %}
      </div>
    
      <div id="content">
        <div class="w-full px-6 md:max-w-4xl md:mx-auto">
          {% block content %}
            {{ content | safe }}
          {% endblock %}
        </div>
      </div>

      <div id="footer" class="py-8">
        <div class="w-full px-6 md:max-w-4xl md:mx-auto">
          {% include "footer.njk" %}
        </div>
      </div>
    </div>

    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-57132539-4"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'UA-57132539-4');
    </script>
  </body>
</html>
